package com.davidm1a2.afraidofthedark.common.entity.mcAnimatorLib.animation

/**
 * Enum of channel mode, each mode makes the channel perform slightly differently
 */
enum class ChannelMode {
    LINEAR,
    LOOP,
    CYCLE,
    CUSTOM
}